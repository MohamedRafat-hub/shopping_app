import 'package:flutter/material.dart';

class AccountView extends StatelessWidget {
  const AccountView({super.key});
  static const id = 'AccountView';
  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
